"""Structured toy software-repair tasks for Report 139."""

from __future__ import annotations

from .models import CHANNELS, RepairCandidate, RepairTask


def r(
    repair_id: str,
    title: str,
    description: str,
    visible: bool,
    hidden: bool,
    regression: float,
    root: float,
    maintainability: float,
    security: float,
    api: float,
    performance: float,
    reviewability: float,
) -> RepairCandidate:
    return RepairCandidate(
        repair_id=repair_id,
        title=title,
        description=description,
        visible_test_result=visible,
        hidden_test_result=hidden,
        regression_risk=regression,
        root_cause_score=root,
        maintainability_score=maintainability,
        security_score=security,
        api_contract_score=api,
        performance_score=performance,
        reviewability_score=reviewability,
    )


TASKS = (
    RepairTask(
        task_id="auth_email_case_boundary",
        bug_report="Users with mixed-case emails cannot log in after signup.",
        visible_failure="A visible login test fails for Alice@Example.com.",
        hidden_cause="Signup normalizes identity keys but login compares the raw email string.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("lowercase_login_only", "Lowercase login input", "Lowercase only the login form value before lookup.", True, False, 0.46, 0.42, 0.72, 0.86, 0.50, 0.90, 0.78),
            r("normalized_identity_boundary", "Normalize identity boundary", "Move canonical email normalization into the shared identity lookup boundary.", True, True, 0.07, 0.97, 0.91, 0.95, 0.94, 0.91, 0.92),
            r("fallback_password_search", "Fallback account search", "Search all accounts when direct lookup fails.", True, False, 0.82, 0.12, 0.28, 0.05, 0.20, 0.20, 0.10),
        ),
        expected_best_repair="normalized_identity_boundary",
        explanation="The best repair fixes the shared identity invariant, not only the visible login case.",
    ),
    RepairTask(
        task_id="profile_cache_stale_update",
        bug_report="Profile edits save successfully but the next page load shows old data.",
        visible_failure="The visible profile test observes stale display text after save.",
        hidden_cause="The profile cache key is not invalidated for the edited user.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("clear_all_cache", "Clear every cache", "Flush the whole application cache after any profile save.", True, True, 0.63, 0.99, 0.48, 0.90, 0.72, 0.18, 0.42),
            r("invalidate_profile_key", "Invalidate profile key", "Invalidate the edited user's profile and derived summary keys.", True, True, 0.08, 0.96, 0.90, 0.93, 0.92, 0.86, 0.91),
            r("disable_profile_cache", "Disable profile cache", "Bypass profile cache reads entirely.", True, True, 0.52, 0.61, 0.62, 0.91, 0.85, 0.24, 0.58),
        ),
        expected_best_repair="invalidate_profile_key",
        explanation="Green visible tests are not enough when the repair creates performance and regression cost.",
    ),
    RepairTask(
        task_id="payment_decimal_rounding",
        bug_report="Invoice totals are off by one cent on fractional tax rates.",
        visible_failure="A visible test fails for 10.005 with sales tax.",
        hidden_cause="Money is represented with binary floating point instead of decimal cents.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("add_round_epsilon", "Add rounding epsilon", "Add 0.00001 before rounding totals.", True, False, 0.58, 0.22, 0.35, 0.90, 0.70, 0.95, 0.22),
            r("decimal_money_boundary", "Use decimal money boundary", "Convert calculation and serialization to decimal cents at the money boundary.", True, True, 0.11, 0.98, 0.86, 0.93, 0.90, 0.84, 0.87),
            r("round_display_only", "Round display only", "Round invoice display strings while leaving stored totals unchanged.", True, False, 0.71, 0.15, 0.30, 0.88, 0.54, 0.96, 0.18),
        ),
        expected_best_repair="decimal_money_boundary",
        explanation="The correct repair preserves the money invariant through hidden calculation paths.",
    ),
    RepairTask(
        task_id="pagination_cursor_boundary",
        bug_report="The final item is missing from paginated API responses.",
        visible_failure="The visible page-two test expects item 50 and receives only items 26-49.",
        hidden_cause="Cursor comparison is exclusive while the stored cursor already points after the last emitted row.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("increase_page_size", "Increase page size", "Request one extra row and trim the response.", True, False, 0.50, 0.30, 0.50, 0.93, 0.45, 0.42, 0.47),
            r("fix_cursor_contract", "Fix cursor contract", "Make cursor encode the last emitted key and update inclusive/exclusive comparisons consistently.", True, True, 0.10, 0.96, 0.88, 0.93, 0.92, 0.88, 0.89),
            r("reverse_default_sort", "Reverse sort default", "Change the default sort order to make the visible test include the missing item.", True, False, 0.86, 0.08, 0.20, 0.88, 0.05, 0.90, 0.08),
        ),
        expected_best_repair="fix_cursor_contract",
        explanation="The weak channel is the API contract, not the visible page size.",
    ),
    RepairTask(
        task_id="flaky_ready_timeout",
        bug_report="The worker-start test flakes in CI.",
        visible_failure="The visible test sometimes checks readiness before the worker has subscribed.",
        hidden_cause="The test and service race on subscription readiness.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("increase_timeout", "Increase timeout", "Raise the CI timeout from 2s to 15s.", True, False, 0.44, 0.18, 0.54, 0.93, 0.80, 0.38, 0.36),
            r("await_ready_event", "Await ready event", "Expose and await the worker readiness event before publishing work.", True, True, 0.09, 0.95, 0.89, 0.93, 0.90, 0.87, 0.90),
            r("retry_publish", "Retry publish", "Retry failed publishes up to five times.", True, False, 0.57, 0.33, 0.46, 0.91, 0.70, 0.45, 0.41),
        ),
        expected_best_repair="await_ready_event",
        explanation="Timeout changes hide the race instead of repairing the readiness contract.",
    ),
    RepairTask(
        task_id="migration_backfill_contract",
        bug_report="Deployment fails after adding a required account_status column.",
        visible_failure="The migration test fails because existing rows have null status.",
        hidden_cause="The migration added a non-null column without a backfill and rollback-compatible default.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("default_new_rows_only", "Default new rows only", "Set a model default for new writes.", True, False, 0.62, 0.99, 0.62, 0.90, 0.62, 0.88, 0.50),
            r("backfill_and_rollback", "Backfill and rollback", "Add staged default, backfill existing rows, validate, then enforce non-null with rollback notes.", True, True, 0.12, 0.98, 0.87, 0.92, 0.91, 0.80, 0.88),
            r("drop_not_null", "Drop not-null", "Remove the required constraint so deployment passes.", True, False, 0.74, 0.20, 0.36, 0.87, 0.42, 0.91, 0.20),
        ),
        expected_best_repair="backfill_and_rollback",
        explanation="The migration has to preserve old data and deployment safety, not only pass the local schema test.",
    ),
    RepairTask(
        task_id="upload_path_traversal",
        bug_report="Upload rejects a path traversal case but still mishandles encoded paths.",
        visible_failure="The visible test catches ../secrets.txt.",
        hidden_cause="Upload validation checks raw strings before canonicalizing and allowlisting destination paths.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("replace_dotdot", "Replace dot-dot", "Reject strings containing ../ before saving.", True, False, 0.50, 0.30, 0.62, 0.35, 0.72, 0.93, 0.52),
            r("canonicalize_allowlist", "Canonicalize and allowlist", "Resolve the path, decode encodings, and require it to stay under the upload root.", True, True, 0.08, 0.97, 0.90, 0.96, 0.91, 0.86, 0.91),
            r("skip_validation", "Skip validation", "Trust object storage to reject dangerous keys.", True, False, 0.90, 0.05, 0.18, 0.02, 0.12, 0.96, 0.06),
        ),
        expected_best_repair="canonicalize_allowlist",
        explanation="Security is the weakest channel; a raw-string patch leaves hidden encodings exploitable.",
    ),
    RepairTask(
        task_id="event_listener_leak",
        bug_report="Repeated navigation triggers duplicate websocket messages.",
        visible_failure="The visible leak test detects multiple message handlers.",
        hidden_cause="The component subscribes on mount but does not unsubscribe on owner teardown.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("remove_all_listeners", "Remove all listeners", "Clear all websocket listeners on component teardown.", True, False, 0.76, 0.44, 0.40, 0.90, 0.28, 0.70, 0.30),
            r("owner_scoped_unsubscribe", "Owner-scoped unsubscribe", "Store the subscription token and unsubscribe only the owner handler on teardown.", True, True, 0.07, 0.96, 0.90, 0.92, 0.93, 0.88, 0.90),
            r("increase_listener_limit", "Increase listener limit", "Raise the listener warning threshold.", True, False, 0.64, 0.10, 0.26, 0.90, 0.70, 0.92, 0.18),
        ),
        expected_best_repair="owner_scoped_unsubscribe",
        explanation="A broad listener clear passes the visible leak check but breaks other subscribers.",
    ),
    RepairTask(
        task_id="orders_n_plus_one",
        bug_report="The order list endpoint is too slow for users with many orders.",
        visible_failure="The visible performance test exceeds its query-count budget.",
        hidden_cause="The endpoint fetches line items inside the per-order loop.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("cache_entire_response", "Cache entire response", "Cache the full order list for each user.", True, False, 0.66, 0.40, 0.48, 0.90, 0.70, 0.82, 0.42),
            r("batch_line_items", "Batch line items", "Fetch orders and line items in two scoped queries and preserve filter semantics.", True, True, 0.09, 0.97, 0.88, 0.93, 0.91, 0.92, 0.89),
            r("raise_budget", "Raise test budget", "Raise the query-count budget to match current behavior.", True, False, 0.55, 0.05, 0.30, 0.90, 0.80, 0.20, 0.08),
        ),
        expected_best_repair="batch_line_items",
        explanation="The repair has to reduce the true query pattern without stale-data risk.",
    ),
    RepairTask(
        task_id="timezone_serialization",
        bug_report="Reminder emails fire one hour late after daylight-saving transitions.",
        visible_failure="The visible date test fails for a local timezone example.",
        hidden_cause="The system stores local wall-clock strings instead of UTC instants plus timezone context.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("subtract_offset", "Subtract offset manually", "Subtract the current local offset before saving reminders.", True, False, 0.68, 0.22, 0.34, 0.90, 0.52, 0.92, 0.18),
            r("utc_instant_with_zone", "UTC instant plus zone", "Store UTC instants with source timezone metadata and convert only at display/send boundaries.", True, True, 0.10, 0.97, 0.86, 0.91, 0.90, 0.84, 0.86),
            r("use_server_timezone", "Use server timezone", "Interpret all reminders in server local time.", True, False, 0.80, 0.09, 0.24, 0.88, 0.20, 0.95, 0.10),
        ),
        expected_best_repair="utc_instant_with_zone",
        explanation="The hidden DST channel rejects visible-only offset arithmetic.",
    ),
    RepairTask(
        task_id="rate_limit_identity",
        bug_report="One abusive client can cause unrelated users behind the same NAT to be throttled.",
        visible_failure="The visible test shows a normal user blocked by IP-only rate limiting.",
        hidden_cause="Rate-limit identity ignores account, route sensitivity, and abuse risk.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("key_by_user_only", "Key by user only", "Switch the rate-limit key from IP to user id.", True, False, 0.55, 0.48, 0.64, 0.46, 0.72, 0.90, 0.56),
            r("composite_risk_key", "Composite risk key", "Use user, IP, route class, and abuse signal with separate anonymous handling.", True, True, 0.11, 0.96, 0.88, 0.93, 0.90, 0.86, 0.88),
            r("disable_rate_limit", "Disable rate limit", "Disable throttling on the affected route.", True, False, 0.88, 0.05, 0.20, 0.02, 0.34, 0.96, 0.08),
        ),
        expected_best_repair="composite_risk_key",
        explanation="The fix must preserve abuse protection while repairing the identity channel.",
    ),
    RepairTask(
        task_id="search_query_injection",
        bug_report="Search accepts quoted input but hidden tests still find injection paths.",
        visible_failure="The visible test covers a single quote in the search term.",
        hidden_cause="The query builder concatenates user input into SQL instead of binding parameters.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("escape_quotes", "Escape quotes", "Escape single quotes before concatenating SQL.", True, False, 0.52, 0.28, 0.56, 0.32, 0.70, 0.88, 0.44),
            r("parameterized_query", "Parameterized query", "Replace string concatenation with query parameters for all search predicates.", True, True, 0.08, 0.98, 0.91, 0.97, 0.92, 0.89, 0.91),
            r("alphanumeric_only", "Alphanumeric only", "Reject non-alphanumeric search terms.", True, False, 0.47, 0.35, 0.50, 0.82, 0.30, 0.93, 0.36),
        ),
        expected_best_repair="parameterized_query",
        explanation="The security channel makes escaping a symptom patch, not a root-cause repair.",
    ),
    RepairTask(
        task_id="frontend_form_state",
        bug_report="The submit button stays disabled after clearing a validation error.",
        visible_failure="The visible UI test clears the input but the button remains disabled.",
        hidden_cause="The DOM value resets, but reducer validation state still marks the field dirty and invalid.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("force_enable_button", "Force-enable button", "Enable the button whenever the DOM field is non-empty.", True, False, 0.60, 0.25, 0.42, 0.90, 0.44, 0.93, 0.28),
            r("sync_reducer_validation", "Sync reducer validation", "Route clear/reset events through the reducer and recompute validation state.", True, True, 0.09, 0.96, 0.89, 0.93, 0.91, 0.90, 0.90),
            r("disable_validation", "Disable validation", "Disable the field validator after the first clear.", True, False, 0.72, 0.10, 0.24, 0.74, 0.22, 0.96, 0.08),
        ),
        expected_best_repair="sync_reducer_validation",
        explanation="The hidden state channel punishes a visible DOM-only fix.",
    ),
    RepairTask(
        task_id="dependency_api_break",
        bug_report="The build fails after a dependency changed a parser API.",
        visible_failure="The visible build test fails with missing parseConfig.",
        hidden_cause="The repo depends on an adapter boundary that was never updated for the new parser API.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("pin_old_dependency", "Pin old dependency", "Pin the dependency to the last version with parseConfig.", True, False, 0.58, 0.99, 0.52, 0.66, 0.76, 0.84, 0.42),
            r("compat_adapter", "Compatibility adapter", "Update the parser adapter and add versioned contract tests around the adapter boundary.", True, True, 0.10, 0.96, 0.88, 0.90, 0.91, 0.86, 0.88),
            r("vendor_parser", "Vendor parser", "Copy the old parser into the repo.", True, False, 0.78, 0.22, 0.20, 0.42, 0.38, 0.55, 0.12),
        ),
        expected_best_repair="compat_adapter",
        explanation="The repair should preserve the adapter contract instead of freezing the dependency path.",
    ),
    RepairTask(
        task_id="inventory_underflow",
        bug_report="Inventory can go negative when two checkouts happen at the same time.",
        visible_failure="The visible test observes a negative count after concurrent checkout.",
        hidden_cause="The code decrements stock outside a transactional reserve/check boundary.",
        correctness_channels=CHANNELS,
        candidate_repairs=(
            r("clamp_to_zero", "Clamp to zero", "Clamp negative inventory values to zero after decrement.", True, False, 0.70, 0.18, 0.32, 0.88, 0.46, 0.92, 0.20),
            r("transactional_reserve", "Transactional reserve", "Reserve stock with a transactional conditional update and reject oversell attempts.", True, True, 0.11, 0.98, 0.88, 0.91, 0.90, 0.84, 0.88),
            r("skip_failed_decrement", "Skip failed decrement", "Ignore failed decrements during checkout.", True, False, 0.82, 0.12, 0.22, 0.70, 0.25, 0.96, 0.08),
        ),
        expected_best_repair="transactional_reserve",
        explanation="The visible negative value is a symptom of a transactional correctness failure.",
    ),
)
